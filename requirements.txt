# GUI (bridge_sim_gui.py) — deal generation + bundled DDS solver.
# Not on PyPI; install from the repo (Windows: use the main-branch ZIP).
#   python -m pip install "git+https://github.com/anntzer/redeal"
# redeal

# GUI toolkit: PySide6 (Qt). QtWebEngine (Chromium) renders the styled report
# inside the window. The PySide6 metapackage pulls Essentials + Addons, which is
# where QtWebEngineWidgets lives.
PySide6>=6.6

# Optional: enables the "Explain" button (Claude verdict). Needs ANTHROPIC_API_KEY.
anthropic

# Headless examples under examples/ use endplay (pip-installable, bundles DDS).
endplay>=0.5
